// const apiKey = "abcd123"; // used for default export
// export default apiKey; // default export that can be renamed on import

export const apiKey = "abcd123"; // used for named export

export const url = "http://theamsalems.com";

export function sayHi(name) {
  console.log(`Hello there ${name}`);
}
